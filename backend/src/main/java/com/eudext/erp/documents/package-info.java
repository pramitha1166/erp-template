/**
 * Documents and Attachments module (PLAT-DOC). Spring Modulith application module —
 * collaborates with other modules only via published events or the
 * types exposed from this package; internal implementation lives under
 * {@code internal} sub-packages (ARCH-1).
 */
@org.springframework.modulith.ApplicationModule(displayName = "Documents and Attachments")
package com.eudext.erp.documents;
