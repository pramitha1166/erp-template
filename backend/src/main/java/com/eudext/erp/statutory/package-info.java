/**
 * Sri Lanka Statutory module (MOD-LK). Spring Modulith application module —
 * collaborates with other modules only via published events or the
 * types exposed from this package; internal implementation lives under
 * {@code internal} sub-packages (ARCH-1).
 */
@org.springframework.modulith.ApplicationModule(displayName = "Sri Lanka Statutory")
package com.eudext.erp.statutory;
