# Backend (Maven)
backend/target/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Local env overrides
.env
.env.local

# Terraform (.terraform.lock.hcl is intentionally NOT ignored — commit it,
# same as a package-lock.json, for reproducible provider versions)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
infra/environments/*/backend.hcl

